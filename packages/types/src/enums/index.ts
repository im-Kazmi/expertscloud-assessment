export enum ProductPriceType {
  Recurring = "recurring",
  OneTime = "one_time",
}

export enum ProductPriceAmountType {
  Fixed = "fixed",
  Custom = "custom",
  Free = "free",
}

export enum SubscriptionRecurringInterval {
  Monthly = "month",
  Yearly = "year",
}
